{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-tests-debug",
            "type": "shell",
            "command": "npm",
            "args": [
                "run",
                "gulp",
                "build-tests-debug"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}