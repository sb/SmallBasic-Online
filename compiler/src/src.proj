<Project>

  <Import Project="..\..\common\common.msbuild.props" />

  <PropertyGroup>
    <WebpackEntryFile>$(MSBuildProjectDirectory)\compiler.ts</WebpackEntryFile>
    <WebpackOutputFile>$(CodebaseRoot)\out\$(Configuration)\compiler\compiler.js</WebpackOutputFile>
    <WebpackTargetType>web</WebpackTargetType>
  </PropertyGroup>
  
  <Import Project="$(CodebaseRoot)\common\common.webpack.proj" />

</Project>